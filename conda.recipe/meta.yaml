package:
  name: fedoo
  version: 1.0.0

source:
  git_url: https://github.com/3MAH/fedoo.git

build:
  noarch: python

requirements:
  host:
    - python
    - setuptools
    - pip
    - scipy
    - matplotlib
    - pandas
    - simcoon
    - pardiso4py
  run:
    - python

test:
  imports:
    - fedoo
