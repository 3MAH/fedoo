package:
  name: fedoo
  version: 1.0.0

source:
  git_url: https://github.com/3MAH/fedoo.git

build:
  noarch: python

requirements:
  host:
    - python
  run:
    - python
    - pandas
    - matplotlib
    - scipy
    - simcoon
    - pardiso4py

test:
  imports:
    - fedoo
